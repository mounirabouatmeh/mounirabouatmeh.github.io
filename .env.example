# Vercel AI Gateway model. Keep one model for the full session.
CUBERENCE_AI_MODEL=openai/gpt-5.6-luna

# Authentication is expected through Vercel AI Gateway OIDC in deployment.
# For non-Vercel/CI use, AI_GATEWAY_API_KEY may be configured instead.
